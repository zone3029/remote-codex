# Changelog

## 0.4.35

- 修复界面开关显示已开启、但系统服务仍提示没有桌面控制权限的问题；授权文件和交互桥接统一使用 ProgramData 共享目录。
- 后台服务启动后自动创建永久远程桌面，中继重启或网络断开后自动重建会话；用户主动关闭后本次运行期间不再自动重启。
- 中继配置文件只在导入时读取，保存后使用软件内部配置；测试连接和重新连接不再要求原文件继续存在。

## 0.4.34

- Windows 客户端启动后默认开启 Codex 界面控制，仍可在界面中手动关闭。
- 远程桌面的默认失效时间改为“永不失效”，会话持续到用户或管理端主动关闭。

## 0.4.31

- RDP 中继在 Agent WebSocket 短暂断开时保留未过期会话和端口，等待 Agent 自动重新接入。
- Windows Agent 在中继断开后显示自动恢复状态，并由轮询循环恢复同一 RDP 会话。

## 0.4.30

- Electron Agent 中转服务器配置和 Worker 热重载。
- 交付包同步包含 Linux、Docker、Codex skill 和部署教程。

## 0.1.0

- Electron Agent 新增中转服务器配置按钮和配置弹窗。
- 支持 HTTPS、注册令牌和证书 SHA-256 指纹校验。
- 支持 Worker 热重载中转配置并重新注册，不重建设备身份。
- 新增 Linux systemd 一键安装/卸载脚本。
- 新增 Docker Compose、随机配置生成脚本和 Nginx 示例。
- 新增 Codex skill、部署参考和中文使用教程。
